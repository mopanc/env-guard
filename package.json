{
  "name": "env-guard",
  "version": "1.0.0",
  "description": "Type-safe environment variable validation. Schema-based, fails fast, zero dependencies.",
  "author": "Jorge Morais",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test tests/*.test.ts",
    "lint": "eslint src/ tests/",
    "audit:security": "npm audit --audit-level=high",
    "check": "npm run build && npm run lint && npm test && npm run audit:security",
    "prepublishOnly": "npm run check"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^25.5.0",
    "eslint": "^9.39.4",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.57.0"
  },
  "keywords": [
    "env",
    "environment",
    "validation",
    "type-safe",
    "config",
    "dotenv",
    "schema"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mopanc/env-guard"
  },
  "engines": {
    "node": ">=18"
  }
}
